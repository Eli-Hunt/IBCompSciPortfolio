import java.time.DayOfWeek;
import java.util.Arrays;
import java.util.List;

import static sun.jvm.hotspot.runtime.BasicObjectLock.size;

@SuppressWarnings("WrongPackageStatement")
public class POTWPartBC<indexPos, p_array, length, totalPerDay, i> {
        public POTWPartBC(Object length) {
                Object length1;
                length1 = length;
                this.length = length1;
        }

        public static void main(String[] args){

}


        List<Integer>passengers=Arrays.asList(100,150,700,20,50,80,70,100,200);
        int[]p_array=new int[size()];
        private Object length;

        private Object i = new Object();

        {
        for(int i = 0; i<size(); i++){
        p_array[i]=passengers.get(i);
        }
        for(int i = 0; p_array;) {
                System.out.print(i + ",");
        }
        System.out.println();


 //C
int daysPerWeek = 0;
if(


                final boolean b;
                if (() >= 7) b = true;
                else b = false;)
                daysPerWeek = 7;


else{
        daysPerWeek = size();

        }
                int[] totalPerDay = new int [daysPerWeek];
                int average = 1;
                for(int i = 0; i < length; i++){
                        if(){
                                average+=1;
                                }
                        totalPerDay[i%daysPerWeek] += (p_array[i]);
                        totalPerDay[i%daysPerWeek] = totalPerDay[i%daysPerWeek]/average;
                
                        }
                for(int i : totalPerDay){
                        System.out.print(i+",");
                
                        }
                System.out.println();

                int max = totalPerDay[0];
                int indexPos = 0;
                for(int i = 0; i<7; i++){
                        if(max<totalPerDay[i]){
                                max = totalPerDay[i];
                                indexPos = i;
                        }
                }

                System.out.println(DayOfWeek.of(indexPos+1);

                POTWPartBC()
                i = 0;


        }
        
        }



